# =============================================================================
# LITELLM PROXY ENVIRONMENT CONFIGURATION
# =============================================================================
# API-Keys werden ausschließlich über die LiteLLM UI verwaltet
# Diese Datei enthält nur Basis-Konfiguration

# =============================================================================
# MASTER KEY FÜR ADMINISTRATIVE OPERATIONEN
# =============================================================================
LITELLM_MASTER_KEY=sk-ki-system-master-2025

# =============================================================================
# UI PASSWORD (Standardwert für erste Anmeldung)
# =============================================================================
UI_PASSWORD=admin123

# =============================================================================
# KEINE API-KEYS HIER!
# Alle LLM-Provider-API-Keys werden über die LiteLLM UI unter:
# http://localhost:4000 verwaltet
# =============================================================================

# Hinweise für API-Key-Verwaltung:
# 1. Öffnen Sie http://localhost:4000 im Browser
# 2. Login mit: admin / admin123
# 3. Navigieren zu "Models" -> "Add Model"
# 4. Fügen Sie Ihre API-Keys für OpenAI, Anthropic, Google hinzu
# 5. API-Keys werden sicher in der PostgreSQL-Datenbank gespeichert

# =============================================================================
# API KEYS (Für lokale Entwicklung - NUR ZU TESTZWECKEN)
# In Produktion: Über LiteLLM UI verwalten!
# =============================================================================

# SCHRITT 1: Ersetzen Sie diese Platzhalter mit echten API-Keys
OPENAI_API_KEY=sk-YOUR-REAL-OPENAI-KEY-HERE
ANTHROPIC_API_KEY=sk-ant-YOUR-REAL-ANTHROPIC-KEY-HERE
GEMINI_API_KEY=YOUR-REAL-GOOGLE-API-KEY-HERE

# =============================================================================
# WICHTIGER HINWEIS:
# Diese Methode ist nur für lokale Tests!
# Für Produktion nutzen Sie ausschließlich die LiteLLM UI!
# =============================================================================
